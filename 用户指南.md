# JM 漫画下载器 v3.2.1 用户指南

by TYZQ

## 发行版快速开始

1. 下载 `JM-Downloader-v3.2.1-Windows-x64.zip` 及同名 `.sha256` 文件。
2. 在 PowerShell 运行
   `Get-FileHash .\JM-Downloader-v3.2.1-Windows-x64.zip -Algorithm SHA256`，
   确认结果与校验文件一致。
3. 解压 ZIP，保持文件夹结构完整，双击 `JM-Downloader.exe`。
4. 进入「搜索与下载」，输入 JM 号或先搜索漫画。

发行版已经包含 Python 和运行依赖。请整体移动 `JM-Downloader` 文件夹，
不要只移动 EXE，也不要遗漏 `_internal`。

- `JM-Downloader.exe`：日常使用的正式版。
- `JM-Downloader-Debug.exe`：显示终端日志，启动异常时使用。

两个入口共用同一套设置、任务和便携数据。程序不会在 AppData、注册表、
Windows 凭据管理器或数据库中创建应用状态。

## 下载漫画

1. 在左侧进入「搜索与下载」。
2. 打开「下载任务」，输入纯数字或带 `JM` 前缀的编号。
3. 程序在后台读取章节目录，完成后打开章节确认框。
4. 对话框默认不勾选任何章节；至少选择一章才能确认。
5. 单章漫画同样需要确认，以防误触或重复下载。
6. 确认后任务进入队列，任务行显示阶段、进度和第一张有效图片。

章节选择不再限制一次勾选数量。选择超过 10 章时，程序会按列表原顺序
每最多 10 章自动拆分为多个任务。摘要会显示“已选 N 章，将创建 M 个
任务”。同一本漫画的多个任务严格串行，不同漫画仍可按全局任务并发数
同时运行。

如果所选章节与已有任务重叠，或存在其他可以提前判断的问题，本批任务
一个也不会创建。对话框会保留当前选择并列出具体问题章节，由用户取消
问题项后再次确认；程序不会自动跳过，也不会静默创建部分批次。

只有进入下载后才能发现的远端坏章会使当前子任务失败，并尽量显示相关
章节；已经完成的其他任务不会回滚，后续非重叠任务继续执行。失败任务
可以移除后重新选择。

### 已下载章节与强制重下

打开确认框时，程序会在后台读取本地章节清单并离线验证图片：格式必须
一致、数量必须与页数相同，每个候选文件都要通过图片有效性检查。

- 完整章节显示“已下载”，默认不可选择。
- 开启「包含已下载章节」后可以选择它们进行强制重下。
- 强制重下先把旧章节暂存；新章节完整验证并发布成功后才替换旧内容。
- 失败或取消会恢复旧章节，不留下半套替换结果。
- 清单缺失、损坏或布局漂移时不会猜测，按未检测到已下载章节处理。

如果全部章节已下载，确认按钮会保持禁用并显示提示。

### 下载引擎与并发

程序默认使用 jmcomic 内置异步下载引擎。设置中可以切换到同步线程
引擎，用于兼容特定网络环境。两种引擎都使用相同的图片临时文件、完整性
校验、章节清单、打包和取消检查点。

默认最多同时下载 2 个任务，每个任务并发 16 张图片。多章漫画默认同时
处理 2 章，也可以选择排队处理（同时 1 章）。同一本漫画因磁盘互斥始终
只运行一个 worker，不会同时修改同一章节清单或打包目录。

暂停与取消属于协作式停止：后续请求不会再发起，但已经发出的网络请求
不保证立即中断。完整返回的图片仍会安全发布。

### 保存方式与图片格式

设置页提供三种保存方式：

| 保存方式 | 结果 |
|----------|------|
| 分章 PDF | 每章一份 PDF，默认选项 |
| CBZ | 每章一份 CBZ，图片按自然顺序存入 |
| 仅图片 | 不生成任何打包产物 |

图片格式可以选择 JPG 或 PNG。PNG 通常体积更大，格式转换也会增加 CPU
与下载完成前的处理时间。CBZ 不会再次压缩已经压缩过的图片。

PDF 与 CBZ 都保存在「打包产物目录」，默认根目录仍叫 `PDFs/`。任务没有
打包产物时也可以正常完成。

### 任务配置快照与恢复

每个新任务会保存创建时的固定章节 ID，以及当时的下载引擎、API 路线、
保存方式、图片格式、图片并发、多章行为和输出目录。之后修改全局设置，
不会改变已有任务；暂停、重启和失败后继续都使用原任务配置。

更早版本中没有固定章节选择的旧任务仍按兼容规则处理：实际单章可以继续，
实际多章会在请求任何图片前失败，需要移除后重新选择。

### 批量管理任务

点击下载任务页的「批量管理」后，任务行会临时显示选择框。退出批量模式
或离开任务页时选择会清空，平时不会永久占用任务行空间。

- **批量暂停**：处理等待、读取目录或下载中的任务。
- **批量继续**：只处理已暂停任务，不会重试失败任务。
- **批量取消**：可以处理等待、运行、暂停和失败任务，只移除任务并保留
  已下载文件，不提供批量删除文件选项。

任务状态可能在点击和实际执行之间变化，因此批量操作会逐项判断并汇总
成功、跳过和失败结果。需要删除本地内容时，仍使用单任务的确认流程。

## 搜索漫画与搜索历史

「搜索结果」提供综合、作者、标签和精确 JM 号查询。上游没有返回的标题、
作者、标签或章节信息不会被程序自行补造。普通搜索最多展示前 1000 条。

设置中的「查询与同步引擎」控制之后开始的普通搜索、作者与标签搜索、
精确 JM 号详情、章节目录读取、收藏完整同步，以及收藏写入成功后的刷新
读取。默认使用「异步查询（推荐）」；遇到特定网络或上游兼容问题时，
可以切换到「同步线程（兼容）」。两条路径使用相同的结果和错误语义，
设置变化不会切换已经在途的操作。

查询与同步引擎独立于下载引擎。搜索封面仍由原有有界加载器处理；账号
登录和收藏写操作继续使用同步客户端；在线阅读器使用自己的异步链路。

卡片以「在线阅读」为主要操作，并保留独立的下载按钮。下载目录读取期间
显示「读取章节…」；单章和多章都要经过章节确认，以避免误触。已有任务
时下载按钮进入任务页面。点击在线阅读不会创建下载任务。

成功返回的搜索会加入搜索历史，空结果也算成功；校验失败、网络失败和
被新请求淘汰的迟到结果不记录。历史最多保留 50 条，同类型同规范化文本
去重，最近使用的记录排在最前。

点击关键词或 JM 号输入框会打开对应历史。选择记录会回填并提交；垃圾桶
按钮用于单独删除。设置页提供「清除搜索历史」，删除前会原生确认。

历史保存在 DPAPI 加密的 `search_history.dat`。它绑定当前 Windows 用户；
复制到其他电脑或用户后通常无法解密，并会按既有恢复语义降级为空历史。

## 在线与本地阅读

### 进入与恢复进度

搜索结果和「我的收藏」卡片提供「在线阅读」；本地漫画库对受管布局中
存在图片的漫画提供 `book` 图标「本地阅读」。点击后打开同一个独立原生
阅读窗口；它不是浏览器或独立进程。阅读窗口非模态，可以调整大小、
最小化或最大化，同时继续操作主窗口。正常关闭后会记住窗口的正常大小
与位置，显示器或 DPI 改变导致位置不可见时会自动移回可用屏幕。

本地入口会先在后台离线检查章节，只列出图片数量、格式和有效性均与清单
一致的完整图片章节。它直接读取受管 JPG/PNG，不访问网络、不写
`ReaderTemp/`，也不读取 PDF 或 CBZ。旧版图片目录必须先使用「识别章节」
迁移为受管布局。

- 没有历史时，单章漫画直接打开；多章漫画先选择起始章节。
- 有历史时，程序恢复同一漫画上次的章节和图片页。
- 历史章节已经失效时，程序会说明原因并让用户重新选择，不猜测替代章。
- 阅读不要求登录；有效账号会话可以只读复用，失效时公开内容可匿名回落。
- 同一本漫画再次打开时只激活当前窗口，不重新载入。
- 打开另一部漫画时会提示保存进度并复用窗口，默认和 Esc 都是取消。
- 同一本漫画在本地与在线来源之间切换时也会提示复用窗口。
- 阅读窗口本身不会被 Esc 关闭，请使用标题栏关闭或「关闭阅读」。

### 阅读与导航

左侧阅读画布使用窗口内全部可用纵向空间。漫画标题、「关闭阅读」、章节、
阅读视图、缩放、重试和下载状态位于右侧控制栏上部；竖向进度条位于中部；
上一章/下一章和上一页/下一页固定在底部。

图片按竖向连续方式排列，页与页之间不留空隙。页码表示章节中的图片序号，
例如 `37 / 120`。拖动竖向进度滑动条会预览目标页，释放后跳转；第一页在
顶部，上一页、下一页与滑动条使用相同页码。

右侧控制栏提供上一章、下一章、章节选择和「下载当前章节」。程序会在后台
核对本地清单和现有任务：只有明确未下载且没有重叠任务时按钮才可提交。
已完整下载、已有等待/运行/暂停/失败任务、仍在检查或检查失败时都不会
误提交；检查失败时按钮改为「重新检查下载状态」。阅读缓存不会成为正式
下载文件。

「阅读视图」有两种全局记忆的布局：

- **适合宽度**：图片铺满阅读区域宽度，适合竖向长图。
- **单页视图**：每张图片完整缩放到一个视口高度的区域并居中，仍保持
  竖向连续滚动。

缩放提供 25%、50%、75%、100%、125% 和 150% 六档。100% 表示当前阅读
视图的自动布局尺寸；125% 和 150% 超出视口时可以水平滚动。阅读视图与
缩放可以在设置页或阅读窗口修改，两个入口会立即同步并持久化。25%、
50% 和 75% 保持对应显示尺寸，但图片解码清晰度不低于 100% 布局基准；
解码宽度仍受 4096 像素上限和约 128 MiB 内存预算保护。

键盘导航与页面按钮使用同一页码：

- `←`、`PageUp`、`Shift+Space`：上一图片页。
- `→`、`PageDown`、`Space`：下一图片页。
- `↑`、`↓`、鼠标滚轮和触控板：保持连续纵向滚动。

两种阅读视图都不会把滚轮改成整页翻动或自动吸附，避免纵向拼接漫画出现
画面割裂。

单页网络或图片错误只在对应页面显示，不会清空已经成功的页面。可以重试
单页，或使用「重试失败页」重试当前章节全部失败项。

### 阅读历史与缓存

阅读进度使用 DPAPI 加密保存在 `reading_history.dat`。同一本漫画只保存
最近一条和最后使用的本地/在线来源，最多保留 100 部，超过后淘汰最久
未阅读的记录。搜索页和收藏页共用同一个「阅读历史」，可以继续阅读、
删除单条或清空全部。退出登录不会删除阅读历史；清空操作需要用户主动
确认。

从历史继续本地阅读前会再次离线检查。本地章节已经缺失、损坏或失效时，
程序询问是否「转为在线阅读」；默认按钮和 Esc 都是取消。取消不会改变
当前阅读窗口，也不会联网或写入虚假进度。

v3.2.0 的阅读历史格式增加本地/在线来源。写入新格式后若降级到 v3.1.0，
旧版会把无法识别的未来版本历史保留为 `.corrupt-*` 备份并显示空历史；
不要删除该备份，重新使用 v3.2.0 后可以按需恢复。

在线图片只在本次程序运行期间缓存：

- 解码后的内存缓存初始预算约 128 MiB。
- `ReaderTemp/` 会话磁盘缓存初始预算约 512 MiB。
- 到达预算后优先淘汰离当前阅读位置较远的页面。
- 正常退出清理当前会话；异常退出遗留目录会在下次启动时清理。
- 阅读缓存不写入 `Pictures/`、`PDFs/` 或章节清单，也不会跨启动复用。

本地图片直接从 `Pictures/` 的受管章节读取，沿用同一个解码内存预算，
不会复制到 `ReaderTemp/`，关闭阅读也不会删除本地漫画。

## API 路线

设置页的「API 路线」提供自动选择和四条固定路线。路线实时影响之后发起的
下载、搜索、账号与收藏请求，但不会改变已经创建任务锁定的路线。

「测试路线」由用户主动点击，在后台执行短超时只读探测，不阻塞界面，也
不会在启动时自动联网。固定路线不可用时请切回「自动选择」，或选择其他
路线后重试。程序不会修改 `option.yml`，也不会把路线选择写入全局域名缓存。

v3.2.1 随 jmcomic 2.7.2 更新了固定路线。升级时，设置和未完成任务中可以
明确识别的四条旧固定路线会迁移为「自动选择」，不会猜测“旧路线 1”
对应哪条新路线。未知或损坏的路线值不会被宽松接受。

## 账号与密码记忆

登录只用于账号和收藏功能，不影响普通搜索与下载。登录成功后会话保存在
DPAPI 加密的 `account.dat`；启动时只恢复本地会话，不会自动联网。

「记住密码」默认关闭。开启时会先显示安全警告，只有用户确认且登录成功
后，账号和密码才写入 DPAPI 加密的 `credentials.dat`。

- 成功的已认证收藏读取如果获得新的有效 Cookie，会写回 `account.dat`。
- 服务器明确判定会话过期、记住密码仍开启且内存中有可用凭据时，程序
  静默登录最多一次，并最多重试一次原收藏读取。
- 静默恢复失败后进入登录过期状态，不会循环重试。
- 应用启动时仍只恢复本地状态，不自动联网或自动登录。
- 登录失败不会覆盖此前保存的有效凭据。
- 成功登录会更新记忆凭据。
- 关闭「记住密码」会立即删除 `credentials.dat`。
- 点击「退出登录」会同时删除记忆凭据、账号会话和收藏缓存，后续不会
  被静默恢复。

DPAPI 可以避免直接读取磁盘上的明文密码，但不代表能够防御当前 Windows
用户会话中的恶意程序。换电脑或换 Windows 用户后通常无法解密。

## 我的收藏

1. 登录后点击「同步」，读取全部收藏夹和全部分页。
2. 只有完整同步成功才会替换本地快照；中途失败继续显示上次完整内容。
3. 使用收藏夹菜单切换“全部收藏”、未分类和自建收藏夹。
4. 本地筛选匹配标题、作者和 JM 号，不联网，也不按标签匹配。
5. 选择收藏时间或更新时间后，需要手动同步才能按新顺序读取远端。
6. 筛选工具栏的「阅读历史」读取本地记录，不依赖收藏同步是否成功。

### 添加、移动与管理收藏夹

搜索结果卡片的收藏按钮会打开「选择收藏位置」。可以选择自建收藏夹或
未分类；取消不会发请求。选择自建收藏夹时，程序先添加、再移动，这两次
远端写入不是原子操作，移动失败会明确提示部分成功。

收藏卡片的文件夹按钮可以移动到其他自建收藏夹，或移回“未分类（默认位置）”。
「管理收藏夹」支持创建新收藏夹，以及删除已经在线预检为空的自建收藏夹；
“全部收藏”和非空收藏夹不能删除。当前不支持取消收藏或重命名收藏夹。

服务器确认新建、删除、添加或移动后，程序会自动刷新完整收藏。写入成功
但刷新失败时保留旧快照并提示稍后手动同步。远端写入遇到断网或结果无法
确认时不会自动重试，也不会伪造本地结果。

## 本地漫画库

本地漫画库扫描当前图片目录和打包产物目录，可以：

- 按 JM 号或按名称检索。
- 按下载时间或名称排序；默认按下载时间从新到旧。
- 筛选全部、有图片或有打包产物的漫画。
- 打开图片目录和打包产物文件夹。
- 使用 `book` 图标阅读受管布局中的完整图片章节。
- 删除单本漫画的图片、全部 PDF/CBZ，或者全部本地文件。
- 进入多选模式并批量删除。
- 对受管布局点击「管理章节」。
- 对旧版图片布局主动点击「识别章节」。

四类图标使用不同语义：`folder` 打开目录，`book` 本地阅读，`menu` 管理
受管章节，`scan` 识别旧目录，不用同一图标表达多个操作。

批量删除会逐本取得任务管理器的本地操作权限，成功和失败分别汇总。每本
漫画继续使用先暂存、成功后清理、失败时回滚的安全删除流程。正在下载或
执行其他本地操作的漫画不会被删除。

「删除打包产物」会删除受管的 PDF 和 CBZ，但不动图片；「删除全部」再
包括图片。漫画级「删除图片」会保留章节清单中的页数、图片格式、原格式
和下载时间，之后仍能检查并重新下载。旧版本生成的旧整本 PDF
`PDFs/JM号.pdf` 永远不会被这些操作触碰。

新布局漫画的标题、章节顺序、页数、图片格式和下载时间来自
`Pictures/JM号/.jm-chapters.json`。排序使用最新章节的完成时间；旧清单
没有该字段时回落漫画目录修改时间。没有清单的旧布局不会联网重建，也不会
猜测标题或章节。

### 管理章节与离线检查

「管理章节」列出每章顺序、标题、有效图片数、图片状态、原格式、打包
状态和下载时间。「重新检查」只读取清单声明的本地路径，不访问网络，
也不会自动修改文件。

- 图片状态分为完整、缺失和损坏。
- 原格式分为 PDF、CBZ、仅图片和待确认。
- PDF/CBZ 状态分为完整、缺失和损坏；仅图片显示无需打包。
- 图片完整而打包产物缺失或损坏时，可以「重建所选」，全程使用该章
  记录的原格式，不受设置页之后的变化影响。
- 图片缺失或损坏时，选择「修复所选」才会创建固定章节强制重下任务；
  不同原格式会分组，每个任务仍最多 10 章。
- 单章失败会列出章节与原因，其他已经成功的章节不会回滚。

旧清单没有原格式时，程序先检查精确的受管产物：只有 PDF 或只有 CBZ
可以确定格式；证据不足或两者并存时会打开原生确认框。当前设置只决定
默认选中项，用户取消后不会写清单或生成产物。「仅图片」不会生成 PDF
或 CBZ。

每章的删除菜单使用一次点击可选的原生菜单，并提供：

- 「删除图片」：保留该章打包产物和清单记录。
- 「删除打包产物」：保留图片、清单记录和原格式；仅图片章节不提供。
- 「删除全部」：删除该章受管内容并移出清单。

删除最后一章的全部内容后，程序只清理已确认的空受管目录；不明额外文件
会阻止清理。删除前必须原生确认，活跃下载任务会阻止本地操作。

### 识别旧版章节

旧版布局不会因打开页面或刷新本地库而联网。只有用户点击「识别章节」并
确认后，程序才使用当前 API 路线读取远端章节目录；这一步不会开始下载。

程序仅接受本地目录名与远端章节名经历史 Windows 清洗规则处理后精确且
唯一对应的方案，并在修改前展示来源、远端章节和新目录预览。重复、遗漏、
歧义、额外文件、损坏图片、链接、路径冲突或不安全路径都会拒绝迁移并保持
磁盘不变。迁移不使用模糊匹配，不提供人工逐章配对，不下载、不自动打包，
也不处理或删除旧整本 PDF。

## 设置

| 设置 | 范围或选项 | 生效方式 |
|------|------------|----------|
| 漫画图片目录 | 相对或绝对路径 | 新任务 |
| 打包产物目录 | 相对或绝对路径 | 新任务 |
| 下载引擎 | 异步下载、同步线程 | 新任务 |
| 查询与同步引擎 | 异步查询（推荐）、同步线程（兼容） | 之后开始的查询/同步 |
| API 路线 | 自动、四条固定路线 | 新请求/新任务 |
| 保存方式 | 分章 PDF、CBZ、仅图片 | 新任务 |
| 图片格式 | JPG、PNG | 新任务 |
| 同时下载 | 1 至 8 个任务 | 保存后调度 |
| 图片并发 | 1 至 64 张图片 | 新任务 |
| 多章行为 | 同时 2 章、排队 1 章 | 新任务 |
| 默认阅读视图 | 适合宽度、单页视图 | 立即同步 |
| 默认缩放 | 25% 至 150%，六档 | 立即同步 |
| 记住密码 | 默认关闭 | 确认后启用 |
| 日志级别 | 调试、信息、警告、错误 | 保存后 |
| 启动页面 | 四个主要页面 | 下次启动 |
| 窗口尺寸、主题 | 支持明亮和黑暗 | 当前会话 |

程序目录内的路径保存为相对路径，整体移动后仍可使用；外部目录保存为绝对
路径。已有任务不跟随设置变化，避免一本漫画被拆到新旧目录。

## 文件输出与便携数据

| 默认路径 | 内容 |
|----------|------|
| `Pictures/JM号/漫画名/` | 单章图片或多章子目录 |
| `PDFs/JM号/漫画名/` | 分章 PDF 或 CBZ |
| `Pictures/JM号/.jm-chapters.json` | 章节清单 |
| `logs/app.log` | 运行日志 |
| `settings.json` | 设置 |
| `tasks.json` | 未完成和失败任务 |
| `account.dat` | 加密账号会话 |
| `favorites.dat` | 加密收藏快照 |
| `search_history.dat` | 加密搜索历史 |
| `credentials.dat` | 可选的加密登录凭据 |
| `reading_history.dat` | 加密阅读进度，最多 100 部 |
| `ReaderTemp/` | 当前运行期的在线阅读会话缓存 |

下载图片先写入 `.jm-part-*` 临时文件，验证后原子替换最终文件。设置、任务
和加密存储也采用原子写入。损坏输入会保留为 `.corrupt-*`，方便恢复和审计。

## 旧版本内容说明

程序不会自动迁移或删除旧内容：

- `PDFs/JM号.pdf` 整本 PDF 保留在磁盘，但本地库不展示、不重建、不删除。
- 没有章节清单的旧图片目录显示「旧版布局，章节信息未知」。
- 新旧布局可以并存；新下载写入受管的新布局。
- 更早版本未保存章节选择的多章任务必须移除后重新创建。

启动程序、进入本地库和读取旧内容都不会自动联网。

## 常见问题

### 正式版双击后没有窗口？

运行 `JM-Downloader-Debug.exe` 查看终端错误，并检查 `logs/app.log`。程序
目录、图片目录和打包产物目录必须可写。

### 固定 API 路线突然全部失败？

进入设置，把 API 路线切回「自动选择」，保存后重试。固定路线可能临时
失效；主动测试只验证当前时刻的可达性。

### 为什么选择很多章节后出现多个任务？

每个任务最多承载 10 章。选择层不限制数量，确认后自动拆分，避免用户反复
打开对话框。同一本漫画的任务会依次执行，不会同时写同一目录。

### 为什么已下载章节不能选择？

它已通过清单和全部章节图片验证。需要重新下载时，开启「包含已下载章节」；
程序会先保护旧文件，新内容完整后才替换。

### 暂停、取消和失败后继续有什么区别？

- 暂停：保留任务和文件，稍后继续固定章节。
- 取消并保留文件：移除任务，不删除磁盘内容。
- 取消并删除：停止同漫画 worker，确认后删除受管内容，其他任务重新排队。
- 失败后继续：复用有效图片，只补缺失或损坏内容。

批量管理中的「继续」只恢复已暂停任务，不等于失败后继续；批量取消始终
保留已经下载的文件。如需删除文件，请退出批量管理并使用单任务确认。

### 为什么选择「仅图片」后没有 PDF 按钮？

这是预期行为。仅图片任务不生成打包产物，完成状态不依赖 PDF 或 CBZ。

### 搜索历史、阅读历史和密码保存在哪里？

搜索历史在 `search_history.dat`；只有开启「记住密码」后，密码才写入
`credentials.dat`；阅读历史在 `reading_history.dat`。三者均由 DPAPI
加密。退出登录会删除记忆凭据，但保留阅读历史。

### 复制到其他电脑能否继续使用？

图片、打包产物、设置和任务可以随完整目录复制。五类 `.dat` 加密文件绑定
原 Windows 用户，另一台电脑通常无法解密，需要清除并重新登录或重新建立
历史。外部绝对路径的下载目录需要单独迁移。

## 源码版（开发者）

源码项目要求 Windows x64 和 Python 3.10 至 3.14：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\setup.ps1
.\.venv\Scripts\python.exe desktop.py
```

`desktop.py` 是唯一生产入口。不要把 `option.yml` 当作用户设置文件修改；
并发、路线、保存格式和图片格式均由应用在内存中注入。

## 构建发行包

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\build.ps1
```

脚本运行测试，构建正式版和调试版，执行启动与离线后端验证，生成
`release/JM-Downloader-v3.2.1-Windows-x64.zip` 及同名 `.sha256`。

构建不会覆盖 v2.1.0、v2.2.0、v2.3.0、v2.4.0、v2.5.0、v2.5.1、
v2.7.0、v2.8.0、v2.9.0、v2.9.1、v3.0.0、v3.1.0 和 v3.2.0 历史
发行包，也会拒绝把设置、任务、账号会话、收藏缓存、搜索历史、阅读历史、
记忆凭据、日志、下载内容和临时文件打入 ZIP。

## 许可与合规说明

本项目部分基于 [JMComic-Crawler-Python](https://github.com/hect0x7/JMComic-Crawler-Python)，
原作者为 hect0x7，并按 MIT License 许可。本项目自身也使用 MIT License，
详情见 [LICENSE](./LICENSE) 和 [THIRD_PARTY_NOTICES.md](./THIRD_PARTY_NOTICES.md)。

Windows 发行版通过 LGPLv3 选项动态加载 Qt 6.11.1、PySide6 Essentials
6.11.1 和 Shiboken6 6.11.1。许可证全文、第三方归属、对应源码及替换库
说明见 `LICENSES/`、[QT_SOURCE_AND_RELINKING.md](./QT_SOURCE_AND_RELINKING.md)
和 [QT_THIRD_PARTY_NOTICES.txt](./QT_THIRD_PARTY_NOTICES.txt)。

阅读器由本项目基于 Qt Widgets 和钉版 jmcomic 接口独立实现；外部
参考项目只用于调查通用产品行为，没有复制其阅读器源代码。

本项目是独立第三方工具，与内容网站及其运营方不存在官方关联。用户应自行
确保使用方式符合适用法律、版权要求、网站服务条款和当地规定。
