# JM 漫画下载器 v2 — 用户指南
by TYZQ

---

## 📦 环境准备（仅首次）

### 第 1 步：一键安装环境

1. 双击 `一键安装.bat`
2. 脚本会检测 Python 3.10–3.14；没有兼容版本时会自动安装内置 Python 3.14.5 并加入 PATH
3. 脚本会创建项目独立的 `.venv`，并按照 `requirements.txt` 安装固定版本依赖
4. 等待依赖检查通过并显示「安装完成」（约 1-2 分钟，需要联网）

### 第 2 步：开启代理（建议）

禁漫天堂对部分 IP 有地区限制。如果你在用 Clash Verge Rev 等代理工具，确保代理已开启即可，程序会自动走系统代理。

---

## 🚀 下载漫画

1. 双击 `一键下载.bat`
2. 浏览器会自动打开页面（如未打开，手动访问 `http://127.0.0.1:58080`）
3. 在输入框中输入禁漫车号（纯数字），按**回车**或点击「添加」
4. 每输入一个车号即可继续添加下一个，最多同时下载 2 个
5. 观察实时进度条，完成后显示「✅ PDF 已生成」

### 界面说明

```
┌─────────────────────────────────────────┐
│  🌸 JM 漫画下载器                        │
├─────────────────────────────────────────┤
│  [输入车号...              ] [添加]     │  ← 逐个输入
├─────────────────────────────────────────┤
│  📖 #1236513  快乐机器性能测试...         │
│  ████████████░░░░░ 65%  下载中 第1话     │  ← 进度实时刷新
│  📖 #789012  配送先の长乳お姉さん...     │
│  ██████████████████ ✅ PDF 已生成        │  ← 完成变绿
│  📖 #888888  某漫画...                   │
│  ░░░░░░░░░░░░░░░░ ❌ 失败  [重试]       │  ← 失败可重试
├─────────────────────────────────────────┤
│  ⏳ 待下载: 1  📥 下载中: 1  ✅ 完成: 1 │  ← 底部统计
└─────────────────────────────────────────┘
```

---

## 📁 文件输出

| 文件夹 | 内容 | 结构 |
|--------|------|------|
| `Pictures/` | 下载的原始图片 | `Pictures/车号/章节名/00001.jpg` |
| `PDFs/` | 自动打包的 PDF | `PDFs/车号.pdf` |

两个文件夹在首次下载时自动创建，无需手动操作。

---

## ⚙️ 高级配置

用记事本打开 `option.yml` 可修改以下参数：

```yaml
download:
  threading:
    batch_count: 16   # 同时下载的图片数（越大越快，越耗带宽）
  image:
    suffix: .jpg      # 图片格式：.jpg / .png
  retry_times: 3      # 失败自动重试次数
```

---

## ❓ 常见问题

### Q: 双击 bat 闪退？
在该文件夹空白处按 `Shift + 右键` →「在此处打开 PowerShell」，输入 `.\.venv\Scripts\python.exe app.py` 回车，查看具体报错。

### Q: 提示 ModuleNotFoundError？
虚拟环境中的依赖不完整。重新双击 `一键安装.bat` 修复。

### Q: 下载报错 Restricted Access / 一直等待？
禁漫限制了你当前的 IP。开启代理后重试。

### Q: 关闭浏览器后任务还在吗？
在。服务器在后台继续运行，重新打开 `http://127.0.0.1:58080` 即可查看进度。

### Q: 怎么获取车号？
在禁漫网站上浏览本子时，网址里的数字就是车号。例如：
`https://18comic.vip/album/1236513/` → 车号 = `1236513`

### Q: 复制到其他电脑能用吗？
可以。目标电脑应为 64 位 Windows 10/11。复制项目时不要复制 `.venv`，但必须保留 `python installer` 文件夹；在新电脑上重新运行一次 `一键安装.bat`。

### Q: 下载速度太慢？
修改 `option.yml` 中的 `batch_count` 值（比如改为 32），并确保代理网络通畅。

---

## 🔧 项目结构

```
JM-Download&wrap_program/
├── app.py                ← 程序入口
├── jm_downloader/        ← API、任务调度、下载及 PDF 模块
├── server.py             ← 旧启动方式兼容入口
├── download_worker.py    ← 旧导入方式兼容模块
├── jpg2pdf.py            ← PDF 命令行兼容入口
├── option.yml            ← 下载配置
├── requirements.txt      ← 固定版本依赖清单
├── static/
│   └── index.html        ← Web 前端界面
├── python installer/     ← 内置 Python 安装程序
├── .venv/                ← 本机环境（自动生成，不参与分发）
├── setup.bat             ← 环境安装实现
├── 一键安装.bat           ← 首次使用入口
├── 一键下载.bat           ← 日常使用入口
└── README.md             ← 技术文档
```

---

## 📜 许可 & 说明

- 基于 [JMComic-Crawler-Python](https://github.com/hect0x7/JMComic-Crawler-Python) 实现下载
- 本工具仅供个人收藏使用，请尊重版权
- 请勿短时间内大量下载，爱护禁漫服务器喵~
